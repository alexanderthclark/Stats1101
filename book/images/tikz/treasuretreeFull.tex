\documentclass{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning, shapes.geometric}
\usepackage{amsfonts}
\begin{document}


   \begin{tikzpicture}[->, grow=right, sloped,
    level 1/.style={sibling distance=2.75cm, level distance = 5cm},
    level 2/.style={sibling distance=1.45cm, level distance=3cm},
    level 3/.style={sibling distance=0.6cm,},
    level 4/.style={sibling distance=0.4cm,}]
    \tikzstyle{bag} = [text width=4em, text centered] % Define bag style
    \tikzstyle{end} = [text width=0em, text centered] % Define end style

    \node[bag] {} % Root node with bag style
        child {
            node[bag] {Island \#1 (X)} % Bag node
                child {
                    node[end, label=right: {Island \#2 (X)}] {} % End node with label
                    edge from parent
                    node[above] {$1$} % Label above the edge
                }
                edge from parent 
                node[above] {$\frac{1}{5}$} % Label above the edge
        }
        child {
            node[bag] {Island \#2 (X)} % Bag node
                child {
                    node[end, label=right: {Island \#3}] {} % End node with label
                    edge from parent
                    node[above] {$1$} % Label above the edge
                }
                edge from parent
                node[above] {$\frac{1}{5}$} % Label above the edge         
        }
        child {
            node[bag] {Island \#3} % Bag node
                child {
                    node[end, label=right: {Island \#4}] {} % End node with label
                    edge from parent
                    node[above] {$1$} % Label above the edge
                }
                edge from parent
                node[above] {$\frac{1}{5}$} % Label above the edge         
        }
        child {
            node[bag] {Island \#4} % Bag node
                child {
                    node[end, label=right: {Island \#5}] {} % End node with label
                    edge from parent
                    node[above] {$1$} % Label above the edge
                }
                edge from parent
                node[above] {$\frac{1}{5}$} % Label above the edge         
        }
        child {
            node[bag] {Island \#5} % Bag node
                child {
                    node[end, label=right: {Island \#1 (X)}] {} % End node with label
                    edge from parent
                    node[above] {$1$} % Label above the edge
                }
                edge from parent
                node[above] {$\frac{1}{5}$} % Label above the edge         
        };
    \end{tikzpicture}



\end{document}
